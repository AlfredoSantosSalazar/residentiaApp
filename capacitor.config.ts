import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'residentiaApp',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
